// Deprecated UI component mistakenly placed inside API route.
// Retained as empty module to avoid build errors while preserving potential import references.
export default function InterviewTranscript() {
  return null;
}
